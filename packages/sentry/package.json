{
  "name": "@whatsapp-ai/sentry",
  "version": "0.1.0",
  "description": "Sentry error tracking and performance monitoring",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sentry/node": "^7.100.0",
    "@sentry/tracing": "^7.100.0",
    "@sentry/profiling-node": "^1.3.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0"
  },
  "peerDependencies": {
    "express": "^4.18.0 || ^5.0.0",
    "fastify": "^4.25.0"
  }
}
